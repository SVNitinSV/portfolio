# Add your photos here (exact filenames)

Put the images you sent into this `assets/` folder using **exactly** these names:

1. `hero-akalya.jpg` → green portrait (hero section)
2. `about-bw.jpg` → black & white portrait with camera (about section)
3. `education-undergrad-group.jpg` → blazer group photo (academic journey)
4. `cert-ntu-classroom.jpg` → NTU classroom group
5. `cert-ntu-outdoor.jpg` → NTU outdoor ivy/stone wall group
6. `cert-aalto-group.jpg` → Aalto group photo
7. `goals-styling.jpg` → styling scene (mint dress)
8. `goals-painting.jpg` → painting on grass
9. `goals-field.jpg` → two-person field portrait

After adding files, run:

```bash
python3 -m http.server 4173
```

Then open:

```text
http://localhost:4173
```
